
proc foo(n: number) {
    if n < 10 {
        log('all good!');
    } else {
        throw Error('it errored!');
    }
}

proc main() {
    try {
        foo(15);
        log('passed');
    } catch e {
        log(e);
    }
}