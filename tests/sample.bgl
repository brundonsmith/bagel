func uuid() => '12345'

const foo: FooType = 'stuff'

func classNames(cn) =>
    cn
        |> entries 
        |> fromEntries


func myFunc(a, b) => 0..10 |> map((n) => n * a) |> filter((n) => n < b)

concat(add(1, 2), 'stuff')

add(concat(1, 2), 12)

myFunc(1, 2)
