{
  "name": "bagel-lib",
  "version": "1.0.0",
  "description": "Runtime library for bagel language",
  "main": "dist.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --outfile=../build/lib.js",
    "watch": "esbuild src/index.ts --bundle --platform=node --outfile=../build/lib.js --watch"
  },
  "author": "Brandon Smith",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "mobx": "^6.2.0"
  }
}
