
// TODO: Or error
export js func parse(str: string): unknown => {# return JSON.parse(str) #}

// TODO: Use types to ensure only valid JSON values make it in here
export js func stringify(val: unknown, indentation: number): string => {#
    return JSON.stringify(val, x => typeof x === 'undefined' ? null : x, indentation)
#}
