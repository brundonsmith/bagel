{
  "devDependencies": {
    "@webpack-cli/generators": "^2.0.0",
    "ts-loader": "^8.1.0",
    "typescript": "^4.2.4",
    "webpack": "^5.31.2",
    "webpack-cli": "^4.6.0"
  },
  "version": "1.0.0",
  "description": "My webpack project",
  "name": "my-webpack-project",
  "scripts": {
    "build": "webpack --mode=production ; cd lib ; npm run build",
    "build:dev": "webpack --mode=development ; cd lib ; npm run build:dev",
    "build:prod": "webpack --mode=production ; cd lib ; npm run build:prod",
    "build-and-run": "webpack --mode=development ; cd lib ; npm run build ; cd ../build ; node compiler.js ../tests/sample.bgl ../tests/sample.js",
    "watch": "webpack --watch",
    "test": "cd tests ; npm run build-and-run"
  }
}
