{
  "devDependencies": {
    "@webpack-cli/generators": "^2.0.0",
    "ts-loader": "^8.1.0",
    "typescript": "^4.2.4",
    "webpack": "^5.31.2",
    "webpack-cli": "^4.6.0"
  },
  "version": "1.0.0",
  "description": "",
  "name": "bagel-compiler",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/index.ts --bundle --platform=node --outfile=../build/compiler.js --external:esbuild",
    "watch": "tsc --noEmit && esbuild src/index.ts --bundle --platform=node --outfile=../build/compiler.js --external:esbuild --watch"
  }
}
